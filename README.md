# VitePress Netlify Forms Demo

## Setup

```sh
# Install dependencies
yarn

# Run local dev server
yarn docs:dev

# Build artifacts for dist
yarn docs:build

# Serve artifacts from dist
yarn docs:serve
```
